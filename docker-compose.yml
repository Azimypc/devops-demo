version: "3.9"

services:
  app:
    image: azimypc/devops-demo:latest   
    container_name: devops-demo-app      
    ports:
      - "3000:3000"                      
    restart: unless-stopped              